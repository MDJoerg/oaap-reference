# Shell scripts and container files must keep LF endings, even when
# checked out on Windows — they are executed on Linux.
*.sh text eol=lf
bin/oaap text eol=lf
Caddyfile text eol=lf
Dockerfile text eol=lf
*.py text eol=lf
*.yml text eol=lf
